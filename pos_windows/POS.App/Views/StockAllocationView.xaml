<Page x:Class="POS.App.Views.StockAllocationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Pembagian Stok">
    <DockPanel Margin="16">
        <TextBlock DockPanel.Dock="Top" Text="Pembagian Stok" Style="{StaticResource TitleText}" Margin="0,0,0,12"/>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Produk:" VerticalAlignment="Center"/>
                <Button Content="Pilih Produk" Click="SelectProduct_OnClick" Margin="8,0"/>
                <TextBlock x:Name="TxtProduct" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Dari Gudang:" VerticalAlignment="Center"/>
                <ComboBox x:Name="CmbFromWarehouse" Width="220" Margin="8,0"/>
                <TextBlock Text="Qty:" VerticalAlignment="Center" Margin="16,0,8,0"/>
                <TextBox x:Name="TxtQty" Width="80"/>
            </StackPanel>

            <TextBlock Grid.Row="2" Text="Ke Gudang:" Style="{StaticResource SubtitleText}" Margin="0,8,0,8"/>
            <ListBox x:Name="ToWarehousesList" Grid.Row="3" SelectionMode="Multiple">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Button Grid.Row="4" Content="Alokasikan Stok" HorizontalAlignment="Right" Margin="0,16,0,0" Click="Allocate_OnClick" Style="{StaticResource AccentButton}"/>
        </Grid>
    </DockPanel>
</Page>
