<Page x:Class="POS.App.Views.ProductsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Produk">
    <DockPanel Margin="16">
        <TextBlock DockPanel.Dock="Top" Text="Manajemen Produk" Style="{StaticResource TitleText}" Margin="0,0,0,12"/>
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <Button Content="Tambah Produk" DockPanel.Dock="Right" Click="Add_OnClick" Style="{StaticResource AccentButton}"/>
            <TextBox x:Name="TxtSearch" KeyDown="TxtSearch_OnKeyDown"/>
        </DockPanel>
        <DataGrid x:Name="ProductsGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nama" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="120"/>
                <DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="100"/>
                <DataGridTextColumn Header="H1" Binding="{Binding PriceH1, StringFormat=N0}" Width="80"/>
                <DataGridTextColumn Header="H2" Binding="{Binding PriceH2, StringFormat=N0}" Width="80"/>
                <DataGridTextColumn Header="Grosir" Binding="{Binding PriceGrosir, StringFormat=N0}" Width="80"/>
                <DataGridTemplateColumn Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Click="Edit_OnClick" Style="{StaticResource LinkButton}"/>
                                <Button Content="Hapus" Click="Delete_OnClick" Style="{StaticResource LinkButton.Danger}" Margin="8,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Page>
