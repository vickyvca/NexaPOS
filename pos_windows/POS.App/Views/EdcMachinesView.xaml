<Page x:Class="POS.App.Views.EdcMachinesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Mesin EDC">
    <DockPanel Margin="16">
        <TextBlock DockPanel.Dock="Top" Text="Manajemen Mesin EDC" Style="{StaticResource TitleText}" Margin="0,0,0,12"/>
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <Button Content="Tambah Mesin EDC" DockPanel.Dock="Right" Click="Add_OnClick" Style="{StaticResource AccentButton}"/>
        </DockPanel>
        <DataGrid x:Name="Grid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nama EDC" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Bank Mapping" Binding="{Binding Bank}" Width="200"/>
                <DataGridTextColumn Header="Lokasi/Kasir" Binding="{Binding Location}" Width="*"/>
                <DataGridTemplateColumn Header="Aksi" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Click="Edit_OnClick" Style="{StaticResource LinkButton}"/>
                                <Button Content="Hapus" Click="Delete_OnClick" Style="{StaticResource LinkButton.Danger}" Margin="8,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Page>
