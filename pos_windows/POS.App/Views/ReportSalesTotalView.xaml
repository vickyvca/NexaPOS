<Page x:Class="POS.App.Views.ReportSalesTotalView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Laporan Penjualan Total">
    <DockPanel Margin="16">
        <TextBlock DockPanel.Dock="Top" Text="Laporan Penjualan Total" Style="{StaticResource TitleText}" Margin="0,0,0,12"/>
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Dari:" VerticalAlignment="Center"/>
                <DatePicker x:Name="DtFrom" Margin="8,0"/>
                <TextBlock Text="Sampai:" VerticalAlignment="Center" Margin="16,0,8,0"/>
                <DatePicker x:Name="DtTo" Margin="8,0"/>
                <Button Content="Terapkan" Click="Apply_OnClick" Margin="8,0" Style="{StaticResource AccentButton}"/>
            </StackPanel>
        </DockPanel>
        <Border Style="{StaticResource CardBorder}" Padding="16">
            <StackPanel>
                <TextBlock Text="Ringkasan Penjualan" Style="{StaticResource SubtitleText}" Margin="0,0,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Subtotal:" FontWeight="SemiBold"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TxtSubtotal" TextAlignment="Right"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Pajak:" FontWeight="SemiBold"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TxtTax" TextAlignment="Right"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Total:" FontWeight="Bold"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="TxtTotal" FontWeight="Bold" TextAlignment="Right"/>
                </Grid>
            </StackPanel>
        </Border>
    </DockPanel>
</Page>
