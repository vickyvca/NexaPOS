<Page x:Class="POS.App.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Login">
    <Grid Background="#f0f2f5">
        <Border Style="{StaticResource Card}" Padding="20" Width="360" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Masuk ke POS Windows" FontSize="18" FontWeight="Bold" Margin="0,0,0,12" HorizontalAlignment="Center"/>
                <TextBlock Text="Username" Margin="0,0,0,4"/>
                <TextBox x:Name="TxtUser" Text="admin" Margin="0,0,0,12"/>
                <TextBlock Text="Password" Margin="0,0,0,4"/>
                <PasswordBox x:Name="TxtPass" Password="admin" Margin="0,0,0,12"/>
                <Button Content="Masuk" Click="Login_OnClick" IsDefault="True"/>
                <TextBlock x:Name="LblMsg" Foreground="Red" Margin="0,8,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
