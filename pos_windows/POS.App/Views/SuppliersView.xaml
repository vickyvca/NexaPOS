<Page x:Class="POS.App.Views.SuppliersView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Supplier">
    <DockPanel Margin="16">
        <TextBlock DockPanel.Dock="Top" Text="Manajemen Supplier" Style="{StaticResource TitleText}" Margin="0,0,0,12"/>
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <Button Content="Tambah Supplier" DockPanel.Dock="Right" Click="Add_OnClick" Style="{StaticResource AccentButton}"/>
        </DockPanel>
        <DataGrid x:Name="Grid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kode" Binding="{Binding Code}" Width="80"/>
                <DataGridTextColumn Header="Nama" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Telepon" Binding="{Binding Phone}" Width="140"/>
                <DataGridTemplateColumn Header="Aksi" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Click="Edit_OnClick" Style="{StaticResource LinkButton}"/>
                                <Button Content="Hapus" Click="Delete_OnClick" Style="{StaticResource LinkButton.Danger}" Margin="8,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Page>
