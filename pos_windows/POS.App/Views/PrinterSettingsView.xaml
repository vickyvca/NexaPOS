<Page x:Class="POS.App.Views.PrinterSettingsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Printer">
    <DockPanel Margin="16">
        <TextBlock DockPanel.Dock="Top" Text="Pengaturan Printer" Style="{StaticResource TitleText}" Margin="0,0,0,12"/>
        <Border Style="{StaticResource CardBorder}" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Ukuran Kertas:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox Grid.Row="0" Grid.Column="1" x:Name="CmbPaper" Width="160" Margin="0,0,0,8">
                    <ComboBoxItem Content="58mm"/>
                    <ComboBoxItem Content="80mm"/>
                </ComboBox>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Printer (USB/Windows):" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox Grid.Row="1" Grid.Column="1" x:Name="CmbPrinters" Width="320" Margin="0,0,0,8"/>

                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,16,0,0">
                    <Button Content="Tes Print" Click="Test_OnClick"/>
                    <Button Content="Simpan" Click="Save_OnClick" Style="{StaticResource AccentButton}" Margin="8,0,0,0"/>
                </StackPanel>

                <TextBlock Grid.Row="3" Grid.Column="1" x:Name="LblMsg" Margin="0,12,0,0"/>
            </Grid>
        </Border>
    </DockPanel>
</Page>
